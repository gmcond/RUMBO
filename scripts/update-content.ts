/**
 * Pipeline semi-automático de actualización de contenido (PRD §M5).
 * Se implementa en la Fase 3: consultará fuentes oficiales whitelisted,
 * extraerá datos con la API de Anthropic y generará changesets en cola de
 * revisión. NUNCA publica directamente.
 */
console.error(
  "update-content llega en la Fase 3 (PRD §M5). " +
    "Este stub existe para reservar el comando npm run update-content."
);
process.exit(1);
